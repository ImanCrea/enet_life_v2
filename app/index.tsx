import { StyleSheet, View, Text } from "react-native";

const Home = () => {
    return (
        <View>
            <Text>Home page</Text>
        </View>
    );
};

export default Home;

const styles = StyleSheet.create({

});